import {
  StyleSheet
} from 'react-native';

const colorBlack = '#000';

export default StyleSheet.create({
  itemContainer:{
    padding:10,
    borderBottomWidth:1
  },
  itemHeader:{
    color: colorBlack,
    fontSize:18,
    paddingBottom:5
  },
  salary:{
    fontSize:20
  }
});
